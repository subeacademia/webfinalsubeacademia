import { inject } from '@angular/core';
import { NavigationEnd, Router } from '@angular/router';
import { filter } from 'rxjs/operators';
import { I18nService } from './i18n.service';

// Ya no se usa para evitar inject() en prerender
export {};

