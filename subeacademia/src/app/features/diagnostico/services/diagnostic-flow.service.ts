import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class DiagnosticFlowService {
  // Este servicio ahora solo mantiene métodos de utilidad si son necesarios
  // La lógica principal se ha movido al componente DiagnosticoComponent
}
